{
  "body": {
    "id"      : "1",
    "name"    : "{{request.body.name}}",
    "items"   : {
        "@repeat" : "item in request.body.items",
        "id"      : "{{$index}}",
        "name"    : "{{item.name}}"
    }
  }
}

